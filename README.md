# TaskFlow API Backend

REST API бэкенд для приложения управления проектами на FastAPI.

## 🛠️ Технологии

- **Python 3.12** — язык программирования
- **FastAPI** — веб-фреймворк
- **SQLAlchemy 2.0** — ORM (async)
- **asyncpg** — асинхронный драйвер PostgreSQL
- **Pydantic** — валидация данных
- **JWT** — аутентификация (python-jose)
- **bcrypt** — хеширование паролей
- **Poetry** — управление зависимостями
- **Docker** — контейнеризация

## 🚀 Запуск

### С Poetry (разработка)

```bash
# Установка зависимостей
poetry install

# Запуск сервера (необходим PostgreSQL)
export DATABASE_URL="postgresql+asyncpg://user:password@localhost:5432/taskflow"
export SECRET_KEY="your-secret-key"
poetry run uvicorn app.main:app --reload
```

### С Docker Compose (рекомендуется)

```bash
# Из корня репозитория - запуск всего стека
cd ..
docker-compose up --build

# Или только бэкенд с БД
docker-compose up --build db backend
```

Сервер доступен на http://localhost:8000

## 📚 API Документация

После запуска доступна интерактивная документация:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **OpenAPI JSON**: http://localhost:8000/openapi.json

## 🔗 Endpoints

### Аутентификация (`/api/auth`)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/register` | Регистрация нового пользователя |
| POST | `/login` | Вход (OAuth2 form, возвращает JWT токен) |
| GET | `/me` | Получить текущего пользователя |

### Проекты (`/api/projects`)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | Список проектов текущего пользователя |
| POST | `/` | Создать новый проект |
| GET | `/{project_id}` | Получить проект по ID |
| PUT | `/{project_id}` | Обновить проект |
| DELETE | `/{project_id}` | Удалить проект |
| POST | `/{project_id}/members` | Добавить участника |
| DELETE | `/{project_id}/members/{user_id}` | Удалить участника |
| GET | `/{project_id}/tasks` | Получить задачи проекта |
| POST | `/{project_id}/tasks` | Создать задачу в проекте |

### Задачи (`/api/tasks`)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/{task_id}` | Получить задачу |
| PUT | `/{task_id}` | Обновить задачу |
| DELETE | `/{task_id}` | Удалить задачу |

### Пользователи (`/api/users`)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/search` | Поиск пользователей по username |

### Системные

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | Информация об API |
| GET | `/healthcheck` | Проверка работоспособности |

## 📊 Модели данных

### User (Пользователь)
- `id` — уникальный идентификатор
- `username` — логин (уникальный, 3-50 символов)
- `hashed_password` — захешированный пароль
- `created_at` — дата создания

### Project (Проект)
- `id` — уникальный идентификатор
- `name` — название (1-100 символов)
- `description` — описание (опционально)
- `owner_id` — ID владельца
- `created_at`, `updated_at` — временные метки

### Task (Задача)
- `id` — уникальный идентификатор
- `title` — заголовок (1-200 символов)
- `description` — описание (опционально)
- `status` — статус: `todo`, `in_progress`, `review`, `done`
- `complexity` — сложность: `low`, `medium`, `high`, `critical`
- `project_id` — ID проекта
- `creator_id` — ID создателя
- `assignee_id` — ID ответственного (опционально)
- `created_at`, `updated_at` — временные метки

### ProjectMember (Участник проекта)
- `id` — уникальный идентификатор
- `project_id` — ID проекта
- `user_id` — ID пользователя
- `joined_at` — дата добавления

## ⚙️ Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DATABASE_URL` | URL подключения к PostgreSQL | — |
| `SECRET_KEY` | Секретный ключ для JWT | — |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Время жизни токена (мин) | 30 |

## 🧪 Тестирование

```bash
# Запуск тестов
poetry run pytest tests/ -v

# С покрытием
poetry run pytest tests/ -v --cov=app
```

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- Аутентификация через JWT токены (Bearer)
- Проверка прав доступа на уровне эндпоинтов
- Валидация входных данных через Pydantic
- Защита от дублирования пользователей (уникальный username)

## 📁 Структура проекта

```
project11b-backend-svobodnaya/
├── app/
│   ├── __init__.py
│   ├── main.py           # Точка входа FastAPI
│   ├── config.py         # Настройки приложения
│   ├── database.py       # Подключение к БД
│   ├── models.py         # SQLAlchemy модели
│   ├── schemas.py        # Pydantic схемы
│   ├── auth.py           # Функции аутентификации
│   └── routers/
│       ├── auth.py       # Роуты авторизации
│       ├── projects.py   # Роуты проектов
│       ├── tasks.py      # Роуты задач
│       └── users.py      # Роуты пользователей
├── tests/
│   └── test_healthcheck.py
├── docs/
│   ├── index.html
│   └── openapi.yaml
├── Dockerfile
├── docker-compose.yaml
├── pyproject.toml
└── README.md
```
